spring:
  profiles:
    active: dev
  jpa:
    hibernate:
      ddl-auto: create-drop
      show-sql: true
app:
  properties:
    security:
      authentication:
        jwt:
          secret: my-secret-token-to-change-in-production
          tokenValidityInSeconds: 86400